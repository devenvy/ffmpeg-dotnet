<Project>
  <Target Name="CopyFFmpegBinaries" AfterTargets="Build">
    <ItemGroup>
      <_FFmpegFiles Include="$(MSBuildThisFileDirectory)..\ffmpeg\**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(_FFmpegFiles)"
          DestinationFiles="@(_FFmpegFiles->'$(OutDir)ffmpeg\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
  </Target>

  <Target Name="PublishFFmpegBinaries" AfterTargets="Publish">
    <ItemGroup>
      <_FFmpegPublishFiles Include="$(MSBuildThisFileDirectory)..\ffmpeg\**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(_FFmpegPublishFiles)"
          DestinationFiles="@(_FFmpegPublishFiles->'$(PublishDir)ffmpeg\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
  </Target>
</Project>
